{
  "name": "cashpot-dev-preview",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && npm install && npm run build && cd ../server && npm install",
    "start": "node server/server.js"
  }
}
